# Dashboard Access

- URL: `http://127.0.0.1:9000/`
- Password: `jessie123`

## Run

```bash
make up
make run
```

## Notes

- Nếu đổi `PASSWORD` trong `.env`, cần chạy lại `make run`.
- Nếu chạy qua máy/VM/WSL khác, có thể cần mở `http://localhost:9000/` hoặc forward port `9000`.
